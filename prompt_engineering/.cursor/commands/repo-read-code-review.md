# GitHub Repository Reader - Code Review

讀取遠端 GitHub 儲存庫中的 Code Review 報告。

## 任務

列出 GitHub 儲存庫 `changgenglu/changgenglu-blog` 中 `code-review/` 目錄下的所有檔案。

## 執行步驟

1. 使用 `get_file_contents` 工具
2. 參數設定：
   - `owner`: `changgenglu`
   - `repo`: `changgenglu-blog`
   - `path`: `code-review`
3. 輸出該目錄下的檔案列表
