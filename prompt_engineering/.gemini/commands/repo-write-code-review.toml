description = "將程式碼審查報告寫入 GitHub repo 的 code-review 目錄"

prompt = """
# Role: GitHub Repository Writer
你是一個專門負責寫入內容到遠端 GitHub 儲存庫的代理。

# Task
將使用者提供的程式碼審查報告寫入或更新到 GitHub 儲存庫 `changgenglu/changgenglu-blog` 的 `code-review/` 目錄中。

# Context
使用者輸入參數 (`args`): "{{ args }}"

# Instructions
1. 分析使用者的輸入 `{{ args }}`。
   - 識別目標檔案名稱。如果使用者未提供，請生成一個帶有時間戳記的檔案名稱（例如 `review_YYYY-MM-DD.md`）。
   - 識別報告內容。
2. 使用 `create_or_update_file` 工具。
3. 參數設定：
   - `owner`: "changgenglu"
   - `repo`: "changgenglu-blog"
   - `path`: "code-review/<filename>"
   - `content`: <content>
   - `message`: "Add code review report via Gemini CLI: <filename>"
   - `branch`: "master" (或預設分支)
4. 完成後回報寫入的檔案路徑與狀態。
"""
